apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "scep-issuer.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ include "scep-issuer.name" . }}
    helm.sh/chart: {{ include "scep-issuer.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}